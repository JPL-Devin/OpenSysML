Handoff — SysML-v2-Go, Plan 8 (def/usage taxonomy) DONE
Project
New independent SysML v2 (incl KerML) implementation in Go → LSP server + CLI REPL. Module github.com/Open-MBEE/Systemica, root /home/han/IdeaProjects/Systems-Modeling, IS a git repo on master (env banner may lie). Go 1.25.10. Pilot reference clone ./SysML-v2-Pilot-Implementation/ — gitignored, nested .git, NEVER commit; stdlib there at sysml.library/; grammars org.omg.kerml.xtext / org.omg.kerml.expressions.xtext / org.omg.sysml.xtext. Spec docs/superpowers/specs/2026-07-25-sysml-v2-go-design.md (15 §, authoritative). docs/ dir + handoff*.md intentionally UNTRACKED.

Status (HEAD 3159758 on master; all tests green + vet clean + -race clean)
Plan 8 (def/usage taxonomy) COMPLETE — see Plan 8 section at end. 7-plan roadmap was already done at 3728d71; def/usage is a post-roadmap feature (own spec + plan cycle). Original status line below reflects the 3728d71 baseline.

Status baseline (HEAD 3728d71 on master; all tests green + vet clean + -race clean)
7-plan decomposition — ALL COMPLETE:
- Plans 1–4 (source+lexer, ast+parser [namespace-core + full expressions; NO def/usage taxonomy — datatype/specializes/part/feature NOT parsed → ErrorNodes], symbols+resolve, passes+diagnostics) — DONE + reviewed
- Plan 5a (workspace/reindex §9) — DONE + reviewed
- Plan 5b (stdlib/cache §10) — DONE + reviewed
- Plan 5c (deps/manifest §11) — DONE (7 commits 1b1493f..c382362; incl atomic libs.Cache.Store)
- Plan 6 (LSP §12) — DONE + reviewed + finished (13 commits 620e16e..7377285)
- Plan 7 (REPL §13) — DONE + reviewed + finished (9 commits dc88aea..3728d71) ← JUST COMPLETED
User-fixed sequence 5b→5c→6→7 fully satisfied. All commits live directly on master (no feature branches; finishing-a-development-branch = no-op each plan). Original 7-plan roadmap is now complete.

Plan 7 recap (what got built, internal/repl/ package + cmd/sysml-repl/)
Dep: github.com/chzyer/readline v1.5.1 (pure-Go readline; history + line editing; unmaintained-since-2018 but stable). Added to go.mod (persists — imported by main.go). NOTE: `go mod tidy` drops any dep not yet imported — a dep added via `go get` before its import will vanish (learned Task 1: chzyer was dropped, re-added Task 8).
cmd/sysml-repl/main.go = thin driver: rlReader adapter over readline.NewEx (HistoryFile os.TempDir()/sysml-repl.history, Prompt "> ", InterruptPrompt "^C" [Ctrl-C→empty line], EOFPrompt "bye" [Ctrl-D→io.EOF→exit]); prints banner; calls repl.Loop(&rlReader{rl}, os.Stdout, repl.NewSession()).
internal/repl/ files:
- session.go: const docName="<repl>"; snippet{src string; names []string}; Session{ws *model.Workspace; snippets []snippet; version int}; NewSession()*Session (ws:model.NewWorkspace()); List()[]string (snippet srcs); accept(src)string (parse src, compute declaredNames, drop earlier snippets whose names intersect [redefine-replaces — MUST physically drop else core emits duplicate-name diags], append, return joined()); joined()=strings.Join(srcs,"\n"); intersects(names,set); Submit(src)Result (declaredNames→accept→version++→ws.Open(docName,joined,version)→ws.Diagnostics→ws.Document.AST.Members, nil-guarded; DECISION documented: submissions ALWAYS accumulated even on parse error — pilot behavior, self-heals via redefine-replace); Clear() (ws.Remove(docName); snippets=nil; version=0).
- declnames.go: declaredNames(root *ast.RootNamespace)[]string — nil-safe; unwrap *ast.Membership→.Member; switch Package/Namespace/Alias→Ident.Name; skip empty. Import/Comment/Dependency/FilterMember declare nothing replaceable.
- continuation.go: needsContinuation(buf)bool — lex via lexer, count {}()[] depth, closers clamp at 0 (stray closers don't go negative), return any>0 at EOF. Trailing ::/= heuristic DEFERRED.
- render.go: Result{Members []ast.Node; Declared []string; Diagnostics []passes.Diagnostic; Source string}; renderSummary(members)[]string; renderMember(m)string (Package/Namespace/Alias/Dependency→"kind "+nameOrAnon, Import→"import "+qnString, Comment→"comment"); nameOrAnon(Identification) (Name, else "<"+ShortName+">", else "<anonymous>"); qnString(*QualifiedName) (nil→"<?>", else Parts joined "::"); renderDiagnostics(diags,src)[]string ("line:col: severity: message" + src line + caretLine; byte-column, multi-byte-rune misalignment documented limitation, LSP owns UTF-16); caretLine(col,spanLen,lineLen) (lead spaces + '^' + '~'*(width-1), width clamped; cosmetic-only overrun when col>>lineLen, never panics); renderResult(r)[]string (diags if any else summary).
- meta.go: isMeta(line)bool (TrimSpace HasPrefix "%"); helpText; (s)runMeta(line)(out []string, quit bool, err error) — %help/%list (or "(empty session)")/%clear (Clear + "session cleared")/%load <file> (os.ReadFile err→wrapped err; else Submit+renderResult); unknown→"unknown command".
- loop.go: const primaryPrompt="> ", contPrompt="... "; LineReader interface{ReadLine(prompt)(string,error)} (io.EOF at end); Loop(r LineReader, out io.Writer, s *Session)error — bufio writer; strings.Builder buf; meta ONLY at empty-buffer primary prompt (mid-buffer % = content); blank line at cont prompt force-flushes; needsContinuation(buf)→contPrompt; submit(w,s,src)=printLines(renderResult(Submit)); printLines writes each+"\n".
Tests: session_test.go, declnames_test.go (parseRoot+equalStrings helpers), continuation_test.go, render_test.go, meta_test.go, integration_test.go (scriptReader + TestLoopEndToEnd: continuation, accumulation, %list, redefine, unresolved-import caret).
Final reviewer verdict: NO BLOCKING ISSUES (verified vs disk; go build/vet/test all pass). Non-blocking: caretLine cosmetic overrun on pathological cols; stdlib not wired at model.NewWorkspace() layer (SAME gap as cmd/sysml-lsp — workspace-level, not REPL-specific; REPL reuses identical machinery so satisfies "as in source files"; user imports across submissions resolve fine); double-parse in Submit (accepted, tiny inputs); %load submits whole file as one snippet.
Roadmap deferrals from Plan 7 (v1 scope): visualization, publish, editor mode, tab-completion, script runner, trailing-operator continuation heuristic, cross-file/stdlib eager auto-load.

Delivered APIs (Plans 1–6, disk-verified — still current)
- parser.New(sf *source.SourceFile)*Parser; (*Parser).ParseFile()*ast.RootNamespace; Parser.Diagnostics []parser.Diagnostic{Span source.Span; Message string}. NO incomplete/EOF continuation signal — continuation via lexer brace-depth only.
- symbols: NewIndex()*Index; (*Index).AddDocument(name,root)/RemoveDocument(name)/AddRecords(name,[]RecordEntry)/LookupQualified(fqn)[]*Symbol/DocumentRoot(name)*Scope; NewIndexFromDoc; Build(root)*Scope; RecordEntry{FQN,Kind,Span}; Symbol{Name;Kind SymbolKind;Decl ast.Node;Visibility;DeclSpan source.Span;Scope *Scope[child,nil for leaves];OwnerScope *Scope}; SymbolKind{SymbolUnknown,Package,Namespace,Alias,Dependency,Comment,Documentation,TextualRepresentation}+String(); Scope: NewScope/Parent/Node/Children/AddChild/Define/LookupLocal/LookupLocalAll/Members/MemberNames.
- libs: DefaultSource()Source; Source interface{List()[]string;Read(name)([]byte,error)}; NewCache()(*Cache,error); Cache keyFor(content)=hex(sha256)+"-v"+formatVersion+Load(key)(*IndexRecord,bool)+Store(key,rec)error (ATOMIC tmp+rename); NewLoader(src,cache)*Loader; (*Loader).Load(name,idx)error.
- deps: manifest.go ParseManifest([]byte)(*Manifest,error) [Dep{Path,Git,Rev,Tag,Branch}; Manifest{LibraryPaths []string; Dependencies map[string]Dep}]; lock.go Lock{SHA map[string]string}/NewLock/ReadLock/(*Lock).Bytes(); fetcher.go Fetcher interface{Fetch(name,dep)(dir,sha,err)}+gitFetcher+fakeFetcher; resolver.go Resolver/NewResolver+resolveDirs+Resolve(root,m,idx) [order WORKSPACE→DEPS→STDLIB, transitive w/ SHA dedup + cycle detect]; load.go loadDir.
- model.NewWorkspace()*Workspace: Open/Update/SetOnDisk/Close/Remove; LookupQualified(fqn)[]*symbols.Symbol (RLock+copied; Index() accessor REMOVED); Document(name)*Document [PUBLIC fields Name/Content/Version/AST/ParseDiagnostics/Scope; sf PRIVATE]; Diagnostics(name)[]passes.Diagnostic [lazy+cached; wraps ParseDiagnostics as Severity=Error Code/Source="syntax" then passes.Analyze]; DocumentNames()[]string; ResolveQualifiedInDoc(name,scope,qn)(*Symbol,bool). Plus EventLoop/Debouncer/Watcher. reindexLocked = FULL reparse each Open.
- LSP core additions: DocumentNames() (workspace.go:159); ResolveQualifiedInDoc (workspace.go:171); symbols.Scope.MemberNames() (scope.go:64); lexer.Keywords() (keywords.go:45).
- resolve.New(idx)*Resolver; ResolveDocument(name,root) APPENDS Resolver.Diagnostics []resolve.Diagnostic{Span;Message}; ResolveQualified/ResolveName/ResolveAliasTarget.
- passes.Analyze(name,root,parseDiags []passes.Diagnostic,idx)[]passes.Diagnostic; passes.Diagnostic{Severity[SeverityError=iota=zero-value],Span,Message,Code,Source}+Severity.String() lowercase.
- source.New(name,[]byte)*SourceFile; .Text(Span)/.Bytes()/.Name()/.Lines().PosAt(offset)Pos{Line,Col} 1-based; Span{Offset,Len}+End().
- ast.Node interface = getters ONLY (Span/LeadingTrivia/TrailingTrivia); setters on *NodeBase only. ast.RootNamespace.Members []Node (elems *Membership|*Import|*Alias|*ErrorNode); Membership{Visibility,Member Node}; Package{Prefixes,Ident Identification,IsLibrary,IsStandard,Members,HasBody}; Namespace{Ident,Members,HasBody}; Alias{Ident,For *QualifiedName}; Import{IsAll,Kind,Imported *QualifiedName,IsRecursive}; Dependency{Ident,Clients,Suppliers}; Comment; Identification{ShortName,ShortNameSpan,Name,NameSpan}; QualifiedName{Global,Parts []NameSegment}; NameSegment{Text,Span}.
- lexer: New(sf)*Lexer; (*Lexer).Next()Token; Token{Kind,Span}; kinds Invalid/EOF/LParen/RParen/LBracket/RBracket/LBrace/RBrace/Semicolon/Colon/ColonColon/Comma/Hash/Dollar/Eq/Keyword; Keywords()[]string; Token.IsTrivia().

LSP gotcha for REPL/future reuse: cache-restored AST-less lib symbols answer QUALIFIED LookupQualified but NOT scope-tree/short-name/hover (force parse via cache-miss path when AST needed).

Standing rules & gotchas
- AGENTS.md caveman-terse chat mode ACTIVE (fragments, drop filler; code/commits/PRs normal).
- Write plan docs OUTLINE-FIRST-THEN-FILL. ONE PLAN AT A TIME just-in-time. ALWAYS verify reviewer/plan grammar+code claims vs pilot grammars + current delivered types before complying.
- Workflow: superpowers subagent-driven-development. Final whole-impl reviewer subagent per plan (VERIFY vs disk). finishing-a-development-branch each plan = no-op (work on master).
- Commit CODE ONLY: docs/ + handoff*.md UNTRACKED. Beware accidental `git add -A` staging them — `git reset` to unstage. Delete stray build binaries (sysml-repl/sysml-lsp) from repo root before committing.
- `go mod tidy` drops deps not yet imported (bit us with chzyer/readline in Task 1).
- zero-value passes.Severity == SeverityError (set explicitly for non-error fixtures).
- (T{}).Method() needs parens in statement/if position.
- structs containing slices are NOT ==-comparable (compare fields).
- first go test slow → go build ./... first, then go test ... -count=1 -timeout 120-180s > /tmp/opencode/x.log 2>&1; tail. internal pkgs UNTESTABLE from /tmp; use -race for concurrency.
- Curated stdlib payload (libs/stdlib/ScalarValues.kerml) is namespace-core only (parses 0 diags); real OMG files deferred (produce ErrorNodes w/o def/usage taxonomy).

What's next (roadmap exhausted — candidate future work, NOT yet planned)
Original 7-plan roadmap complete. Possible next directions (require a new spec/plan cycle, NONE started):
- def/usage taxonomy parsing (datatype/specializes/part/feature) — the biggest known parser gap; currently → ErrorNodes.
- Wire stdlib auto-load into model.NewWorkspace() (affects both LSP + REPL) so bare stdlib refs resolve without explicit import.
- Cross-file references in LSP references.go.
- Real OMG stdlib ingestion (blocked on def/usage taxonomy).
- REPL v2 deferrals: tab-completion, visualization, publish, script runner.

Commit chain tail (HEAD 3728d71)
...7377285(lsp integration test) → dc88aea(repl skeleton) → e50ead7(accumulate/replace) → c9a5a6a(continuation) → 1f6a6ea(eager submit) → db4c194(render summary) → 63c1fd3(render diagnostics) → bb52b9e(meta cmds) → 072746f(readline loop) → 3728d71(repl integration test).

═══════════════════════════════════════════════════════════════════════
PLAN 8 (def/usage taxonomy) — IN PROGRESS. Stuck: PLAN DOC not yet written.
═══════════════════════════════════════════════════════════════════════

PLAN 8 (def/usage taxonomy) — COMPLETE. Date 2026-07-29.
Spec docs/superpowers/specs/2026-07-29-def-usage-taxonomy-design.md (approved). Plan docs/superpowers/plans/2026-07-29-plan-08-def-usage.md (2240 lines, 10 tasks). Both UNTRACKED.
Commit chain (11, all on master, parent 3728d71): 0071a67(T1 ast nodes+dump) 538ee91(T2 lexer :> :>> ::> => munch) 44104a4(T3 parser dispatch+modifiers) b8d0b0e(T4 relationships+multiplicity+value) 639a655(T5 nested bodies+flip fixtures) e0c1a92(T6 symbols kinds+buildDecl) b370b50(T7 libs Supers) 5f1eed6(T8 resolve resolveDecl) 4e8c163(T9 passes TypeCheckPass LevelType) 87ec22f(T10 e2e workspace test) 3159758(docs: stale comment). HEAD 3159758.
Final whole-slice reviewer verdict: NO BLOCKING ISSUES (build/vet/test/-race all green vs disk).

DELIVERED (def/usage v1 = part + attribute only, full pipeline depth):
- ast/defusage.go: enums DefinitionKind{DefPart,DefAttribute}/UsageKind{UsagePart,UsageAttribute}/RelationshipKind{RelTyping,RelSpecializes,RelSubsets,RelRedefines,RelReferences,RelCrosses}/FeatureDirection{DirNone,DirIn,DirOut,DirInOut} (all +String()); Relationship{NodeBase,Kind,Target *QualifiedName}; Multiplicity{NodeBase,Lower Node,Upper Node,IsRange bool} — single bound stored in .Lower (doc-comment STALE, says Upper); Definition{NodeBase,Prefixes,Kind,IsAbstract,IsVariation,Ident,Relationships,Members,HasBody}; Usage{NodeBase,Prefixes,Kind,IsAbstract,IsReference,Direction,IsComposite,IsDerived,IsOrdered,IsNonunique,Ident,Relationships,Multiplicity *Multiplicity,Value Node,Members,HasBody}.
- lexer: 4 compound ops ColonGt(:>)/ColonGtGt(:>>)/ColonColonGt(::>)/EqGt(=>), maximal-munch in `:`/`=` cases; bare `>` unchanged (short-name close safe).
- parser/defusage.go: atDefUsageStart/parseDefUsage/parseDefinition/parseUsage/parseFeatureModifiers/parseRelationships/relationshipClauseKind/parseMultiplicity/parseMultiplicityBound/parseDefUsageBody/parseBodyMember; wired into parseDeclaration tagless switch + 14 declStartKeywords entries. Symbolic :> is context-sensitive (RelSpecializes on def, RelSubsets on usage — isUsage bool). Multiplicity bound parsed at parseBinary(precAdditive) NOT ParseExpression (else `..` at precRange gets swallowed). * → ast.LiteralInfinity.
- symbols: SymbolPartDef/SymbolAttributeDef/SymbolPartUsage/SymbolAttributeUsage + buildDecl cases (child scope per def/usage; anon usage → child scope, no named symbol, double-guarded).
- resolve/document.go: resolveDecl cases resolve relationship targets + multiplicity bounds + value + recurse children.
- libs/record.go: symRecord.Supers populated from RelSpecializes/RelSubsets/RelRedefines raw target text only (typing/references/crosses excluded).
- passes/typecheck.go: TypeCheckPass at LevelType (kind-compat table, unresolved targets SKIP, Diagnostic Code/Source="type"), registered in DefaultRegistry after NameResolutionPass. First inhabitant of the type tier.
NO AST visitor infra — 4 manual type-switch traversals (ast/dump.go dumpNode, symbols/builder.go buildDecl, resolve/document.go resolveDecl, passes/typecheck.go walk) ALL handle the new nodes.

DEFERRALS / NEXT-AGENT NOTES (v1 scope):
- Other ~13 def/usage kinds (item/port/connection/action/state/calc/constraint/requirement/…) — mechanical repeats of the part/attribute pattern (add enum value + keyword-map entry + SymbolKind + compat rule).
- Transitive/multiplicity/abstract semantic checks not done.
- PREFIX METADATA GAP: parser never populates Definition/Usage .Prefixes (parseDefUsage doesn't call parsePrefixMetadata; #-prefixed decls dispatch only to package/namespace). Always nil for def/usage; dump/resolve handle defensively. Minor spec §3.3/§3.4 completeness gap.
- Test-coverage gaps (code correct): keyword forms redefines/references/crosses/'defined by' and single-bound [*] lack direct assertions (only symbolic :>>/::>/=> and [0..*] tested).
- stdlib auto-load still not wired at model.NewWorkspace() layer (same gap as cmd/sysml-lsp).

═══════════════════════════════════════════════════════════════════════
BELOW: original Plan 8 planning notes (spec decisions + change checklist). Superseded by the DELIVERED record above; kept for reference.
═══════════════════════════════════════════════════════════════════════

WHERE WE WERE (pre-implementation resume point — HISTORICAL)
- User chose next work = def/usage taxonomy parsing (biggest known parser gap).
- brainstorming skill run → SPEC WRITTEN + USER-APPROVED: docs/superpowers/specs/2026-07-29-def-usage-taxonomy-design.md (332 lines, 8 §, no placeholders). UNTRACKED (docs/ gitignored).
- writing-plans skill → plan doc written OUTLINE-FIRST then filled, then executed via subagent-driven-development.

SPEC DECISIONS LOCKED (all in the spec; summary)
- Scope = VERTICAL SLICE, 2 kinds only (part + attribute), FULL depth AST→parser→symbols→resolve→type-pass→tests. Other ~13 kinds deferred (mechanical repeats: add enum value + keyword).
- AST = OPTION A generic Definition{Kind DefinitionKind} + Usage{Kind UsageKind} discriminated nodes (minimizes churn across the 4 manual traversals).
- Relationships keyword + SYMBOLIC: typing :/'defined by'; specializes/:>; subsets/:>; redefines/:>>; references/::>; crosses/=>. Multiple targets/clause → one Relationship per target. `:>` symbolic = specializes on a DEF, subsets on a USAGE (context-decided).
- Modifiers: abstract, variation (def-only); ref, in/out/inout, composite/portion, derived, ordered, nonunique (usage). Multiplicity [n]/[lo..hi]/[*] (bounds reuse expr AST; * → LiteralInfinity). Feature value '= expr' reuses expr parser, resolved.
- Supers (libs/record.go:20 symRecord.Supers []string) populated from RAW target text of specializes/subsets/redefines only (NOT typing/references/crosses).
- NEW LevelType pass (passes/typecheck.go) = first inhabitant of empty type tier; kind-compat table; SKIP unresolved targets (nil-guard); Severity=Error Code/Source="type".

CHANGE CHECKLIST (spec §8, 11 items = files touched)
1. ast/defusage.go (NEW): enums DefinitionKind{DefPart,DefAttribute}/UsageKind{UsagePart,UsageAttribute}/RelationshipKind{RelTyping,RelSpecializes,RelSubsets,RelRedefines,RelReferences,RelCrosses}/FeatureDirection{DirNone,DirIn,DirOut,DirInOut}+String() each; Relationship{NodeBase,Kind,Target *QualifiedName}; Multiplicity{NodeBase,Lower Node,Upper Node,IsRange bool}; Definition{NodeBase,Prefixes []*PrefixMetadata,Kind,IsAbstract,IsVariation,Ident Identification,Relationships []*Relationship,Members []Node,HasBody}; Usage{NodeBase,Prefixes,Kind,IsAbstract,IsReference,Direction,IsComposite,IsDerived,IsOrdered,IsNonunique,Ident,Relationships,Multiplicity *Multiplicity,Value Node,Members,HasBody}.
2. ast/dump.go: add cases *Definition/*Usage/*Relationship/*Multiplicity to dumpNode switch (currently ends line 143 default). Helpers avail: qnString(qn), identName(id), writeChildren(b,depth,kids), indent, prefixesAnd(prefixes,members). Note writeChildren closes the open paren; leaf nodes (Relationship/Multiplicity) print inline like Comment (no writeChildren) OR use writeChildren for recursive Members.
3. lexer/token.go: add Kind consts ColonGt/ColonGtGt/ColonColonGt/EqGt + entries in kindNames map (line 72). Existing: Colon(65),Eq(61),ColonColon(59),Gt(38),DotDot(41 — `..` is ONE token, confirmed).
4. lexer/lexer.go: maximal-munch in Next() operator switch. `:` case (lines 49-55): currently `::`→ColonColon else Colon; extend — after detecting `::` peek(2)=='>'→ColonColonGt (pos+=3); for single `:` peek(1)=='>' then peek(2)=='>'→ColonGtGt(pos+=3) else ColonGt(pos+=2). `=` case (lines 81-91): add peek(1)=='>'→EqGt(pos+=2) (before the bare Eq fallthrough; keep === and == first). Bare `>` (line 111 case) UNCHANGED — munch only triggers from leading :/::/=, never bare >, so short-name close `>` safe. Also add these bytes already in canStartToken (`:`,`=` present). ADD to lexer_test.go: munch cases + disambiguation.
5. parser/namespace.go: parseDeclaration switch (line 142, default line 166→nil) add def/usage detection. Detection order: consume feature modifiers (parseFeatureModifiers) → read kind kw part/attribute → peekN for keyword "def" → parseDefinition vs parseUsage. New fns: parseDefinition(start,prefixes,mods)*ast.Definition; parseUsage(start,prefixes,mods)*ast.Usage; parseFeatureModifiers() (small struct of flags, consumes leading modifier kws); parseRelationships()[]*ast.Relationship (loop over specializes/:> /subsets/redefines/:>> /references/::> /crosses/=> / : /'defined by' then parseQualifiedName, comma-list → one Relationship per target; symbolic :> kind from def-vs-usage context); parseMultiplicity()*ast.Multiplicity (`[` expr (`..` expr)? `]`; `*`→LiteralInfinity — build via existing expr node); parseDefinitionBody()([]ast.Node,bool) (like parseNamespaceBody but per-member dispatch also tries def/usage then falls back to namespace member; wrap in *ast.Membership when visibility present). Grammar order: [prefixes][modifiers] kind [def] Identification [relationships] [multiplicity] [= value] (body|';'). declStartKeywords map (line 204) ADD: part,attribute,def,abstract,variation,ref,in,out,inout,composite,portion,derived,ordered,nonunique. FLIP fixtures: parser/recovery_test.go:10 ('part def X; package P;' currently asserts Members[0]=ErrorNode) + parser/namespace_test.go:133 (TestParseFileUnknownKeywordErrorNode 'part def Vehicle;' asserts ErrorNode+diag) → assert real Definition; keep a separate genuinely-malformed fixture proving recovery still resyncs. Parser harness API (parser.go): p.advance()/p.atKeyword(kw)/p.acceptKeyword(kw)/p.at(k)/p.accept(k)(tok,ok)/p.accept2(k)bool/p.expect(k,msg)(tok,ok)/p.peek()/p.peekN(n)/p.error(span,msg)/p.spanFrom(start)/p.takeTrivia()/p.src.Text(span)/p.ParseExpression()/p.parseIdentification()/p.parseVisibility()/p.parseQualifiedName()/p.parsePrefixMetadata(). NEW parser/defusage_test.go table+golden ast.Dump.
6. symbols/symbol.go: SymbolKind enum (line 13, ends SymbolTextualRepresentation line 21) ADD SymbolPartDef,SymbolAttributeDef,SymbolPartUsage,SymbolAttributeUsage + symbolKindNames map entries (line 24).
7. symbols/builder.go: buildDecl switch (line 47) ADD case *ast.Definition (map Kind→SymbolKind; newSymbol(d.Ident,kind,d,vis,child,scope,trivia); if HasBody: child:=NewScope(scope,d), defineIdent, scope.AddChild(child), buildMembers(child,d.Members)) + case *ast.Usage (same; ANONYMOUS usage empty name → still make child scope for members but SKIP defineIdent). newSymbol/defineIdent/NewScope signatures per file. Relationship/Multiplicity/Value are NOT symbols.
8. libs/record.go: collectScope (line 43) populate symRecord.Supers from sym.Decl if *ast.Definition/*ast.Usage → raw target text (qnString-style A::B::C) of Relationships with Kind RelSpecializes/RelSubsets/RelRedefines. (May need a tiny helper; symRecord.Supers field already exists line 20.)
9. resolve/document.go: resolveDecl switch (line 51) ADD case *ast.Definition (for each Relationship: r.ResolveQualified(scope,rel.Target); recurse child via r.childScope(scope,d)+r.walkMembers) + case *ast.Usage (resolve each rel.Target; r.resolveExpr(scope,mult bounds) + r.resolveExpr(scope,d.Value); recurse members). Entry pts: r.ResolveQualified(scope,qn)(*symbols.Symbol,bool) [memoized], r.resolveExpr(scope,node), r.childScope(scope,decl)*Scope [matches by c.Node()==decl].
10. passes/typecheck.go (NEW) + passes/analyze.go: new Pass, Level()==LevelType. Register in DefaultRegistry (analyze.go:11, after NameResolutionPass). Template = nameres.go (Pass interface: Level()PassLevel + Run(ctx *Context,name string,root *ast.RootNamespace)[]Diagnostic). Walk AST mirroring resolveDecl scope-descent: root scope via ctx.Index.DocumentRoot(name); for each Relationship whose target RESOLVES (ctx.Resolver().ResolveQualified(scope,rel.Target) — memo already populated by name-res pass; returns *symbols.Symbol w/ .Kind), check kind-compat table vs source node kind; emit Diagnostic{Severity:SeverityError,Span:rel.Span(),Message,Code:"type",Source:"type"}. Unresolved targets SKIP (nil-guard). Tier gating (registry.go:28) already skips LevelType when name-res errored. Compat table (spec §6.3): RelSpecializes source=def→target must be def of SAME kind; RelSubsets/RelRedefines source=usage→target usage compatible; RelTyping (: T on usage)→part usage wants part def, attribute usage wants attribute def/value type; RelReferences/RelCrosses→light usage check. Cross-kind (part def specializes attribute def) → error.
11. Tests: lexer_test.go (munch), parser/defusage_test.go (+FLIP recovery_test.go/namespace_test.go), symbols/*_test.go (kinds, nested child-scope members by qualified name, anon usage no name binding, Supers), resolve/*_test.go (targets resolve, unresolved→diag, nested Car::engine, value refs), passes/typecheck_test.go (happy, violations, skip-when-unresolved, tier gating), integration REPL smoke (part def Car{part engine:Engine;} clean; kind violation→type caret).

KEY CONSTRAINTS (spec §2.3)
- NO AST visitor infra — 4 manual type-switches (dump.go:40, builder.go:47, document.go:51, NEW typecheck.go) must ALL get the new nodes or silent no-op.
- ALL def/usage keywords ALREADY lex as Kind==Keyword (part/def/attribute/specializes/subsets/redefines/references/crosses/abstract/variation/ref/in/out/inout/composite/portion/derived/ordered/nonunique). NO keyword-table change. Match via atKeyword, not identifiers.
- Symbolic ops :> :>> ::> => are NOT single tokens today (lex as Colon+Gt etc.) — item 4 fixes.
- LevelType/LevelConstraint tiers exist, ZERO passes today; registry gating ready.

PLAN FORMAT TEMPLATE (match plan-06/07): H1 title + spec ref; blockquote "REQUIRED SUB-SKILL: superpowers:subagent-driven-development"; **Goal**/**Architecture**/**Tech Stack**; ## Scope (In/Deferred/Constraints); ## File Structure (per-file one-liner + Tests + Modified); ## Task N — title with `- [ ]` TDD steps (write failing test → run verify FAIL → implement → run verify PASS → gofmt+vet+commit). No placeholders — real code in every step. Suggested task split: T1 AST nodes+enums+dump; T2 lexer compound ops; T3 parser modifiers+dispatch+parseDefinition/parseUsage; T4 relationships+multiplicity+value; T5 nested bodies+flip fixtures; T6 symbols buildDecl+kinds; T7 libs Supers; T8 resolve resolveDecl; T9 typecheck LevelType pass; T10 integration+final review+handoff.
